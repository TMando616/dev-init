<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Retention Period
    |--------------------------------------------------------------------------
    |
    | 退会（ソフトデリート）から完全削除までの日数。この期間内であれば同じメール
    | アドレスで復会でき、学習進捗を引き継げる。期間を過ぎたアカウントは
    | users:purge-deleted コマンドが物理削除する。
    |
    | 退会画面の文言・復会の可否判定・削除バッチの3箇所が同じ値を見るように、
    | ここを唯一の根拠とする。
    |
    */

    'retention_days' => (int) env('ACCOUNT_RETENTION_DAYS', 30),

    /*
    |--------------------------------------------------------------------------
    | Reactivation Token Expiry
    |--------------------------------------------------------------------------
    |
    | 復会リンクに含めるトークンの有効期限（分）。パスワードリセットの
    | config/auth.php passwords.users.expire に揃えてある。
    |
    */

    'reactivation_token_expire' => (int) env('ACCOUNT_REACTIVATION_TOKEN_EXPIRE', 60),

];
